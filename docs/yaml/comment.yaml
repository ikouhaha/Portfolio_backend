


post:
  tags:
    - Comments
  description:
    Create a new comment.
  requestBody:
    description:  new comment data.
    required: true
    content:
      application/json:
        schema:
          $ref: ../json/comment.json#/definitions/comment
  responses:
    '201':
      description: Comment created
    '500':
      description: server error
