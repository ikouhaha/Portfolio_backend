get:
  tags:
    - Breeds
  description:
    Get all breeds.
  responses:
    '200':
      description: View of breeds 
      content:
        application/json:
            schema:      # Request body contents
              $refs: ../responseJson/breed.json
    '500':
      description: server error

